apiVersion: v1
kind: Pod
metadata:
  labels:
    app: milvus-test-client
  namespace: chaos-testing
spec:
  containers:
  - name: main
    image: harbor.milvus.io/qa/fouram:1.9.4
    command:
    - cat
    tty: true
    resources:
      limits:
        memory: 64Gi
        cpu: "16.0"
      requests:
        memory: 64Gi
        cpu: "8.0"
    volumeMounts:
    - name: db-data-path
      mountPath: /root/milvus_ingest_data
    - name: kubeconfig
      mountPath: /root/.kube/config
      subPath: config
  volumes:
  - name: kubeconfig
    configMap:
      defaultMode: 420
      name: qa-kubeconfig
  - name: db-data-path
    nfs:
      server: 172.16.70.249
      path: /volume1/test/milvus_ingest_data